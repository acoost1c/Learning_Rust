const TAX_RATE: f64 = 7.25; // You have to manually declare the data type! VERY IMPORTANT!!!
/* Constants are written in all CAPS */
fn main() {
    let income: i32 = 100000; // It is also fine to manually specify the data type.
    println!("My income is {income}. Tax rate is: {TAX_RATE}%.")
}
